---
title: Индекс
blog: blog
---

%div
  %article.article
    %header
      %h1.entry-title Индекс
    %ul#category-list
      - blog('blog').tags.each do |tag, articles|
        %li
          = link_to "#{tag} (#{articles.count})", tag_path(tag)
